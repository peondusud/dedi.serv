[Unit]
Description=rTorrent
Requires=network.target local-fs.target

[Service]
Type=forking
User=%i
ExecStart=/usr/bin/screen -d -m -S rtorrent /usr/local/bin/rtorrent
ExecStop=/usr/bin/killall -w -s 2 rtorrent

[Install]
WantedBy=multi-user.target
